# Built binary (same name as the module, so `go build` with no -o drops it here)
/claude-runway
# GoReleaser output
/dist/
# Nix build symlink
/result
# Coverage profile. CI writes this on every run (`go test -coverprofile=coverage.out`),
# and CONTRIBUTING tells contributors to run the same command locally.
/coverage.out
